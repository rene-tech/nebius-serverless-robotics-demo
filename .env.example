SERVER_PORT=8787
DEMO_MODE=offline
CORS_ORIGIN=*
VITE_API_BASE_URL=

# Live Nebius Serverless AI endpoint configuration.
# These values are read only by the backend proxy.
NEBIUS_ENDPOINT_URL=
NEBIUS_ENDPOINT_PATH=/v1/robotics/plan
NEBIUS_HEALTH_PATH=/health
NEBIUS_ENDPOINT_TOKEN=
NEBIUS_ENDPOINT_KIND=robotics
NEBIUS_VLM_MODEL=qwen2.5-vl-3b
NEBIUS_VLM_TEMPERATURE=0
NEBIUS_VLM_MAX_TOKENS=900
NEBIUS_VLM_PLAN_STRATEGY=scene-grounded
NEBIUS_MODEL_IMAGE=registry.example.com/robotics/cosmos-reason:demo
NEBIUS_GPU_CLASS=GPU-backed Serverless AI endpoint
NEBIUS_SHM_SIZE=16Gi

# Keep true for event safety: live failures fall back to replay output.
ALLOW_FALLBACK_ON_ERROR=true
COLD_START_THRESHOLD_MS=2500
LIVE_CHECK_TIMEOUT_MS=15000

# Optional rehearsal capture. Keep this local; do not commit generated replay files.
RECORD_LIVE_RESPONSES=false
REPLAY_RECORDINGS_PATH=server/replay-recordings.jsonl
